import React from 'react';

class ReactJSMySQL extends React.Component {
    constructor(props) {
        super(props);
        this.state= {}
    }
    render() {
        return(
            <div>ReactJSMySQL

                Table data from MySQL = JsonData

                getJsonData
                setJsonData

                Send to MySQL

                MVC 
                    model
                        mô tả cấu trúc của dữ liệu
                    controller
                        hành động với cấu trúc dữ liệu đã có
                    view
                        hiển thị dữ liệu từ kết quả của hành động

            </div>
        )
    }
}

export default ReactJSMySQL;